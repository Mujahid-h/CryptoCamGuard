import React from "react";
import { Text, View } from "react-native";
import DefaultLayout from "../components/DefaultLayout";

const Gallery = () => {
  return (
    <DefaultLayout>
      <View>
        <Text>Saved images will appear here</Text>
      </View>
    </DefaultLayout>
  );
};

export default Gallery;
